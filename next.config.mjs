/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  // basePath: "/home",
  // images: {
  //   remotePatterns: [{

  //   }]
  // }
};

export default nextConfig;
